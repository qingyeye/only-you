<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.only.you.dao.ApiMapper">

    <!--<select id="getTest" resultType="java.lang.String">
        select user_id from app_user limit 1
    </select>-->
    <update id="getTest" parameterType="String">
        update app_user set user_name = '王五' where id = #{id}
    </update>
</mapper>